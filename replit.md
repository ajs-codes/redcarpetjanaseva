# Running the project

This is a static HTML site. The Replit preview is served from the project root
with Python's built-in HTTP server:

```bash
python3 -m http.server 5000 --bind 0.0.0.0
```

The configured `Start application` workflow runs this command automatically.
Open the Replit web preview to view `index.html`.